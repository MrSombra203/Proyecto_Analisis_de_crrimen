@{
    ViewData["Title"] = "Acceso Denegado";
    Layout = "_Layout";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-lg border-danger">
                <div class="card-header bg-danger text-white text-center py-4">
                    <h2 class="mb-0">ACCESO DENEGADO</h2>
                </div>
                <div class="card-body text-center p-5">
                    <div class="mb-4">
                        <div style="font-size: 5rem; color: #dc3545;">🔒</div>
                    </div>
                    
                    <h3 class="text-danger mb-3">No tienes permiso para acceder a esta página</h3>
                    
                    <p class="lead text-muted mb-4">
                        Lo sentimos, pero no tienes los permisos necesarios para acceder a esta sección del sistema.
                    </p>

                    <div class="alert alert-warning">
                        <h6 class="alert-heading">
                            ¿Qué puedes hacer?
                        </h6>
                        <ul class="text-start mb-0">
                            <li>Verifica que estés usando una cuenta con los permisos adecuados</li>
                            <li>Contacta al administrador del sistema si crees que esto es un error</li>
                            <li>Si eres administrador, verifica que tu sesión esté activa</li>
                        </ul>
                    </div>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-center mt-4">
                        <a asp-controller="Home" asp-action="Index" class="btn btn-primary btn-lg">
                            Volver al Inicio
                        </a>
                        <a asp-controller="Auth" asp-action="Login" class="btn btn-outline-secondary btn-lg">
                            Iniciar Sesión
                        </a>
                    </div>
                </div>
            </div>

            <div class="text-center mt-4">
                <p class="text-muted">
                    Si necesitas acceso a esta funcionalidad, por favor contacta al administrador del sistema.
                </p>
            </div>
        </div>
    </div>
</div>

<style>
    body {
        background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
        min-height: 100vh;
    }

    .card {
        border-radius: 15px;
        overflow: hidden;
    }

    .card-header {
        border-bottom: none;
    }

    .btn-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        border: none;
        transition: transform 0.2s;
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(0,0,0,0.2);
    }

    .alert-warning {
        background-color: #fff3cd;
        border-color: #ffc107;
    }
</style>
