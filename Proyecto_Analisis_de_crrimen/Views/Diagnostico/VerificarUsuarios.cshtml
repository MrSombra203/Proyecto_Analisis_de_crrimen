@{
    ViewData["Title"] = "Diagnóstico de Usuarios";
}

<div class="container mt-4">
    <h2>Diagnóstico de Usuarios y Contraseñas</h2>
    <p class="text-muted">Esta página ayuda a diagnosticar problemas de autenticación</p>

    <div class="table-responsive mt-4">
        <table class="table table-bordered table-striped">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Usuario</th>
                    <th>Activo</th>
                    <th>Rol</th>
                    <th>Password (preview)</th>
                    <th>Coincide Admin123!</th>
                    <th>Coincide User123!</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var diag in ViewBag.Diagnosticos as List<dynamic>)
                {
                    <tr>
                        <td>@diag.Id</td>
                        <td>@diag.NombreUsuario</td>
                        <td>@(diag.Activo ? "Sí" : "No")</td>
                        <td>@diag.Rol</td>
                        <td><small>@diag.PasswordEnBD</small></td>
                        <td>
                            @if (diag.CoincideAdmin123)
                            {
                                <span class="badge bg-success">Sí</span>
                            }
                            else
                            {
                                <span class="badge bg-danger">No</span>
                            }
                        </td>
                        <td>
                            @if (diag.CoincideUser123)
                            {
                                <span class="badge bg-success">Sí</span>
                            }
                            else
                            {
                                <span class="badge bg-danger">No</span>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <div class="alert alert-info mt-4">
        <h5>Información:</h5>
        <ul>
            <li>Las contraseñas se almacenan en texto plano según la estructura de la base de datos</li>
            <li>Verifica que los usuarios existan en la base de datos</li>
            <li>Verifica que las contraseñas en la BD sean las correctas</li>
            <li><strong>Usuario admin:</strong> Admin123!</li>
            <li><strong>Usuario usuario1:</strong> User123!</li>
        </ul>
    </div>

    <a asp-controller="Home" asp-action="Index" class="btn btn-secondary">Volver</a>
</div>



